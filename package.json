{
  "name": "bb-notes-app",
  "version": "1.0.0",
  "description": "Bread & Butter — A minimal personal notes app",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "npx nodemon src/server.js",
    "db:migrate": "npx prisma migrate dev --name init",
    "db:generate": "npx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "nodemon": "^3.1.7"
  }
}
